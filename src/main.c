#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    int r;
    int a;

    a = 10;
    r = _abs(a);
    printf("%d\n", a);
    printf("%d\n", r);
    return (0);
}